# Diabetes Prediction App

This project provides a Streamlit web application for predicting diabetes using 22 different machine learning models trained on the Pima Indians Diabetes dataset.

## Project Structure
- models/ → contains 22 trained models (.pkl, .joblib, .h5)
- app.py → Streamlit application
- requirements.txt → Python dependencies

## Run Locally
Install the required packages:

